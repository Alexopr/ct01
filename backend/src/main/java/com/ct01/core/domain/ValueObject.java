package com.ct01.core.domain;

/**
 * Маркерный интерфейс для всех Value Objects в системе.
 * Value Objects - это неизменяемые объекты, которые определяются только своими атрибутами,
 * а не идентичностью.
 * 
 * Принципы Value Objects:
 * - Неизменяемость (immutable)
 * - Равенство по значению, а не по ссылке
 * - Отсутствие идентичности
 * - Валидация в конструкторе
 */
public interface ValueObject {
    
    /**
     * Валидация значения. Должна вызываться в конструкторе каждого Value Object.
     * Бросает исключение если значение невалидно.
     */
    default void validate() {
        // Базовая реализация - можно переопределить в конкретных классах
    }
} 
